export const metadata = {
  title: 'My Page Title',
  description: 'Description of my page',
};
export default function Meta() {
  // Your component logic (if needed)
  return null; // This component now does not render anything itself
}
